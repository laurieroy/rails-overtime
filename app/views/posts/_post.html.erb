		<tr>
			<td><%= post.id %></td>
			<td><%= post.overtime_request%></td>
			<td><%= post.date %></td>
			<td><%= post.user.name.sorted.upcase %></td>
			<td><%= link_to truncate(post.rationale), post %></td>
			<td>
				<%= status_badge post.status %>
			</td>
			<td><%= link_to edit_post_path(post), id: "edit_#{post.id}" if policy(post).update? do %>
						<i class="fas fa-edit text-primary"></i>
					<% end %>
						<%
=begin%>
 <i class="bi bi-pencil text-primary"></i> 
<%
=end%>
				
			</td>
			<td><%= link_to post_path(post),
						method: :delete, data: { confirm: "Are you sure?"},
						id: "delete_post_#{post.id}_from_index" do %>
							<i class="fas fa-trash-alt text-danger"></i>
						<% end %>
			</td>
		</tr>